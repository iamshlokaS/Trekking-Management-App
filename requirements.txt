# Trekking Management Application - Requirements
# Flask Backend Framework
Flask==2.3.3
Flask-SQLAlchemy==3.0.5
Flask-Login==0.6.2

# WSGI Utilities
Werkzeug==2.3.7
click==8.1.7
itsdangerous==2.1.2

# Template Engine
Jinja2==3.1.2

# Database - SQLite ONLY (No PostgreSQL)
# SQLite is built into Python, no additional package needed

# NOTE: These packages are NOT included (they violate the SQLite-only requirement):
# - psycopg2-binary  (PostgreSQL driver) ❌
# - gunicorn         (Production server) ❌
# - python-dotenv    (Environment variables) ❌

# All UI assets (Bootstrap, CSS) are loaded from CDN in templates
# No additional JavaScript dependencies needed
